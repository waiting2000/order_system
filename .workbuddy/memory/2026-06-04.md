# 2026-06-04

## WebSocket 实时同步
- 安装 `ws` 包用于服务端 WebSocket
- 创建 `server/ws.js`：WebSocket 服务器挂载到 HTTP Server 同端口 `/ws` 路径，Ping/pong 保活，支持 broadcast 广播
- 修改 `server/index.js`：`app.listen` → `http.createServer(app)` + `server.listen`，共享 HTTP Server
- 修改 `server/routes/api.js`：POST/PUT/DELETE 成功后调用 `broadcast()` 广播事件
  - recipe_added, recipe_updated, recipe_deleted
  - order_added, order_removed, orders_cleared
- 创建 `src/context/WSContext.jsx`：前端 WebSocket 客户端，登录后自动连接，断线自动重连（指数退避 1s→2s→4s...最大 30s），提供连接状态和事件回调
- 修改 `src/context/MenuContext.jsx`：监听 WS 事件自动更新 recipes/todayOrders 状态（静默更新，不弹 toast）
- 修改 `src/App.jsx`：AuthGate 内嵌 WSProvider，顶部 header 添加连接状态指示灯（绿/黄/灰）
- `npm run build` 验证通过

## 菜谱详情弹窗
- 新建 `src/components/RecipeDetail.jsx` 组件
  - 底部滑出动画弹窗（移动端全宽，PC 端居中 max-w-lg）
  - 亮点：大图区域 16:10 比例，图片加载失败回退 Emoji
  - 内容：菜名、分类、烹饪时间、难度、食材标签、备注（支持换行）
  - 底部操作栏：关闭 + 加入/取消今日菜单
  - 交互：点击遮罩关闭、ESC 关闭、滚动锁定背景
- 更新 `src/pages/MenuBrowser.jsx`：卡片点击打开详情弹窗
- 更新 `src/pages/TodayMenu.jsx`：列表项点击打开详情弹窗
- `npm run build` 验证通过

## 历史记录归档页
- 新增后端 API（`server/routes/api.js`）：
  - `GET /api/history/dates`：返回历史日期列表（去重、倒序、限 90 天）
  - `GET /api/history/:date`：返回指定日期的点菜记录
- 新建 `src/pages/History.jsx`：
  - 按日期分组，点击展开/折叠
  - 展开时懒加载当天数据（缓存已加载的日期）
  - 按菜品聚合显示（同一道菜多人点时显示昵称标签）
  - 日期格式化：今天/昨天/前天 + 星期
- 底部导航新增「记录」标签（日历图标），`App.jsx` TABS 数组新增 `history` 项
- `npm run build` 验证通过

## 所有功能开发完成 ✅
- [x] WebSocket 实时同步
- [x] 菜谱详情弹窗
- [x] 历史记录归档页
